# 前端优化与设计依据

## 设计结论

页面采用“中文主叙事 + 英文专业术语”的双语策略。目标用户是中文软件工程推免评委，因此任务、状态、错误和按钮优先使用中文；`Evidence Ledger`、`Diff`、`Live API` 等领域术语保留英文，方便展示技术语义。

## 公开参考

2026-08-28 检索了 GitHub 高星开源设计项目：

- shadcn/ui：约 122k Star；强调开放代码、组件一致性、可访问性；
- Material UI：约 99k Star；强调完整状态系统和设计一致性；
- Mantine：约 31k Star；强调组件完整度与开发体验；
- Headless UI：约 28k Star；强调无样式、可访问交互原语；
- Radix Primitives：约 19k Star；强调高质量状态和键盘交互；
- Anthropic `frontend-design` Skill：所在仓库约 172k Star，强调结构承载信息、克制动效、明确空状态与错误状态。

本项目没有复制这些库的页面代码，也没有引入 React 构建链。原因是考核项目需要核心逻辑清晰、安装简单、便于答辩；当前原生 HTML/CSS/JS 已能落实其设计原则，强行引入大型组件库只会增加依赖和解释成本。

## 本轮落地

1. **消除闪烁**：轮询时只追加新事件，不再每 250 ms 重建全部 DOM；Diff 和验证结果仅在内容变化时更新。
2. **移除外部字体**：取消 Google Fonts，使用 Windows 本地字体，避免字体加载抖动和离线失效。
3. **明确空状态**：告诉用户先选择项目并启动，而不是只展示装饰性空白。
4. **明确失败状态**：API Key、上传格式和运行错误直接显示解决方法。
5. **中文信息优先**：页面操作不要求评委先理解全英文界面。
6. **真实项目入口**：新增 ZIP 导入、项目选择、语言画像和建议测试命令。
7. **可访问性**：加入键盘焦点样式和 `prefers-reduced-motion` 支持。
8. **视觉收敛**：减少渐变、噪点、发光和装饰性图标，让边框、字段和执行顺序承担信息组织作用。

## 2026-08-29 去模板化复核

本轮重点检查页面是否出现常见的 AI 产品模板痕迹，并做了以下调整：

- 将主标题改为具体的工程承诺：“让每次代码修改，都有据可查”；副标题直接说明先复现、再修改、最后测试的顺序。
- 将 `PROOF LOOP / READY / LIVE` 等偏宣传性的组合改为“执行顺序 / RUN ORDER”和“事件记录 / RUN LOG”。英文只保留 `Diff`、`Evidence Ledger`、`Control Path` 等确有技术指向的术语。
- 将“证据置信度”改为“验收证据评分 / EVIDENCE SCORE”，明确分数来自四个验收条件，而不是模型自报的主观信心。
- 补充“未越过工作区边界”这一安全条件，使界面展示与后端 4×25 分的评分逻辑一致。
- 移除星芒 Logo、噪点纹理、玻璃拟态和大面积霓虹阴影，改用清晰的工程面板、细边框和低对比度蓝绿色标记；品牌标记改为“边界框 + 记录点”，对应隔离工作区中的可追踪事件。

当前页面的记忆点不再依赖装饰，而是依赖一个容易现场讲清楚的关系：左侧输入任务，中间记录实际动作，右侧计算验收结果，底部展示 Diff 与测试输出。

## 视觉签名

页面保留的视觉签名是品牌旁的“边界框 + 记录点”，表达任务在隔离副本内留下可追踪记录。编号 01–05 不是纯装饰，而是对应项目输入、执行记录、验收结果、代码改动和回归测试这一真实顺序。
