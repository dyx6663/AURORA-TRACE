AURORA TRACE — Evidence-First Local Coding Agent

The canonical project overview is in README.md.
This plain-text entry is kept for tools that cannot render Markdown.

Quick start:

    python aurora.py

Then open http://127.0.0.1:8765 in a browser. See README.md for the
architecture, verification model, safety boundaries, and test commands.
